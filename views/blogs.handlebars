<div class="d-flex row justify-content-center p-2 text-center">
  {{!-- {{#log blogData}}{{/log}} --}}
  {{#each blogData}}
    <div class="col-8" style="margin-bottom: 20px">
      <a class="list-group-item" href="/blogs/{{id}}">
        <div class="card text-center">
          <div class="card-header">{{post_title}}</div>
          <div class="card-body">
            <div class="card-title">By:
              {{user.username}}
              at
              {{format_date date_created}}
            </div>
            <p class="card-text">
              {{post_contents}}
            </p>
          </div>
        </div>
      </a>
    </div>
  {{/each}}
  <div class="col-6 card">
    <form class="new-blog-form p-3">
      <div class="p-2">
        <input type="text" class="form-control" placeholder="Title" id="post_title" />
      </div>
      <div class="p-2 py-2">
        <textarea class="form-control" placeholder="Details" id="post_contents"></textarea>
      </div>
      <button class="buttons" type="submit">Post New</button>
    </form>
  </div>
</div>

<script src="/js/add-post.js"></script>